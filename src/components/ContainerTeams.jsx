import React from 'react';

import '../assets/styles/components/ContainerTeams.css';

const ContainerTeams = ({children}) => (
  <main className="mainContainer">
    {children}
  </main>
);

export default ContainerTeams;